---
import Section from "../components/Section.astro";

interface Props {
  languages: any[];
  lang: 'es' | 'en';
}

const { languages, lang } = Astro.props;
---

<Section title={lang === 'es' ? 'Idiomas' : 'Languages'}>
  <ul>
    {
      languages.map(({ language, fluency }) => (
        <li><span>{language}:</span> {fluency}</li>
      ))
    }
  </ul>
</Section>

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  li {
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 0;
    text-wrap: pretty;
  }

  li span {
    color: #444;
    font-weight: bold;
  }