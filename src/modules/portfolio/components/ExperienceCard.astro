---
import Terminal from "src/shared/icons/Terminal.astro";
import CalendarIcon from "src/shared/icons/CalendarIcon.astro";
import { highlightText } from "src/shared/utils/highlightText";

interface Team {
  teamName: string;
  role: string;
  stack: string[];
  summary: string;
}

interface Props {
  lang: "es" | "en";
  name: string;
  position: string;
  url: string;
  startDate: string;
  endDate: string;
  summary: string;
  highlights?: unknown[];
  teams?: Team[];
}

const { lang, name, position, url, startDate, endDate, summary, highlights, teams } = Astro.props;
---

<article>
  <header>
    <h2>{position}</h2>
    <div class="company-wrapper">
      <Terminal />
      <a href={url} target="_blank">{name}</a>
    </div>
    <div class="period-wrapper">
      <CalendarIcon />
      <time>{startDate} - {endDate ? endDate : "Present"}</time>
    </div>
  </header>
  <div class="content">
    {summary &&
      <p>{summary}</p>
    }

    {highlights &&
      <div class="highlights-section">
        <h3>{lang === "es" ? "> Logros Clave:" : "> Key Achievements:"}</h3>
        <ul class="highlights">
          {highlights.map((highlight: unknown) => <li>{highlightText(highlight)}</li>)}
        </ul>
      </div>
    }

    {teams &&
      <div class="teams-section">
        <h3>{lang === "es" ? "> Equipos:" : "> Teams:"}</h3>
        {teams.map(({ teamName, role, stack, summary }: Team) => <>
          <h4>{"▸ "} {teamName}</h4>
          <h6>{role} | {stack && stack.join(" • ")}</h6>
          <p>{summary}</p>
        </>)}
      </div>
    }
  </div>
</article>

<style>
  article {
    /* Colors */
    --card-border: var(--red-muted);
    --card-border-hover: var(--cyan-glow);
    --bg-base-card: var(--bg-base);
    --card-heading: var(--red-accent);

    max-width: 1140px;
    width: 100%;
    border: 2px solid var(--card-border);
    border-radius: 5px;
    transition: all 0.3s ease;
    background-color: var(--bg-base-card);
    z-index: 1;
  }
  article:hover {
    border: 2px solid var(--card-border-hover);
    background-color: var(--bg-field);
  }
  header {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
    padding: 1rem 2rem;
    border-bottom: 2px solid var(--card-border);
    background-color: var(--bg-raised);
    transition: all 0.3s ease;
  }
  article:hover > header {
    border-bottom: 2px solid var(--card-border-hover);
    background-color: var(--bg-base);
  }
  h2 {
    color: var(--card-heading);
    transition: color 0.3s ease;
  }
  a {
    color: var(--red-accent);
    text-decoration: none;
    transition: color 0.3s ease;
  }
  article:hover h2, article:hover .company-wrapper, article:hover .company-wrapper a {
    color: var(--cyan-accent);
    transition: color 0.3s ease;
  }
  a:hover {
    text-decoration: underline;
  }
  .company-wrapper, .period-wrapper {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: color 0.3s ease;
  }
  .company-wrapper {
    color: var(--red-accent);
  }
  .period-wrapper {
    color: var(--red-muted);
    transition: color 0.3s ease;
  }
  time {
    font-family: var(--font-terminal);
    font-size: 1.05rem;
    letter-spacing: 0.02em;
  }
  article:hover .period-wrapper {
    color: var(--cyan-muted);
    transition: color 0.3s ease;
  }
  .content {
    padding: 1rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    color: var(--red-core);
    transition: color 0.3s ease;
  }
  article:hover .content {
    color: var(--cyan-core);
    transition: color 0.3s ease;
  }
  .highlights-section, .teams-section {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }
  .highlights {
    list-style-type: "▸ ";
    padding-left: 0.8rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .teams-section p, .teams-section h6 {
    padding-left: 0.8rem;
  }
  h3 {
    font-family: var(--font-terminal);
    color: var(--red-accent);
    font-weight: lighter;
    font-size: 1.4rem;
    transition: color 0.3s ease;
  }
  article:hover h3 {
    color: var(--cyan-accent);
    transition: color 0.3s ease;
  }
  h6 {
    color: var(--red-muted);
    transition: color 0.3s ease;
  }
  article:hover h6 {
    color: var(--cyan-muted);
    transition: color 0.3s ease;
  }
</style>